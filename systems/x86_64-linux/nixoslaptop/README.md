# Historical NixOS host `nixoslaptop`

This machine is now Omarchy (`darrint-laptop`). These NixOS and home-manager files are **not applied**.

Live capture/restore lives in `machines/darrint-laptop/`.
